node_modules/
.wrangler/
dist/
.dev.vars
*.log
.DS_Store

# Editor / OS / AI-tool clutter — never part of project state.
.vscode/
.idea/
.cursor/
.claude/
Thumbs.db
desktop.ini

# Generated by `wrangler types` — regenerate locally/CI, don't track.
worker-configuration.d.ts

# Session planning / spec / research docs — organized locally, not tracked.
docs/

# Local icon drop zone — the app copies live in web/static/icons/
/icons/

# Local pack-image drop zone — the app copies live in web/static/packs/
/screenshots/
